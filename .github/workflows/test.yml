name: 'Test'

on:
  issue_comment:
    types: [created]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
    - name: Dump GitHub context
      id: github_context_step
      run: echo '${{ toJSON(github) }}'